NB: nelle cartelle PIGS, PIMC e VMC sono presenti i dati che vengono utilizzati nel jupyter notebook. In particolare di riferiscono alle tre tecniche nel caso in cui la funzione di prova è la somma di due gaussiane con parametri ottimizzati, trovata nell'esercizio 08.1

NOTA BENE : all'interno del programma è possibile modificare il potenziale esterno (SI RICORDI DI MODIFICARNE ANCHE LE DERIVATE IN QUESTO CASO) e la funzione di prova (SI RICORDI DI MODIFICARNE ANCHE LA DERIVATA SECONDA), che può essere quella costante oppure quella a parametri ottimizzati.

Gli altri file che sono presenti in questa cartella sono TUTTE LE COMBINAZIONI POSSIBILI tra PIGS/PIMC e funzione d'onda prova costante o ottimizzata (CONST/OPT).

Ho verificato che l'energia potenziale delle particelle ad inizio e fine catena nel PIGS, quando la funzione d'onda è quella costante, è l'energia di GROUND STATE VERA del sistema, ovvero -0.46 nelle unità utilizzate.